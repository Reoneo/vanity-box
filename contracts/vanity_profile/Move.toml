[package]
name = "vanity_profile"
edition = "2024.beta"
version = "1.0.0"

# Published address - UPDATE THIS after deployment
# published-at = "0x..."

[dependencies]
# IOTA framework dependency - required for Table, UID, etc.
Iota = { git = "https://github.com/iotaledger/iota.git", subdir = "crates/iota-framework/packages/iota-framework", rev = "framework/mainnet" }

# TODO: Add IOTA Names dependency once the package address is discovered
# IotaNames = { ... }

[addresses]
vanity_profile = "0x0"
# TODO: Replace with actual IOTA Names package address
# iota_names = "0x..."
